name: video_editor
description: A complete video editor built with Flutter and ffmpeg.wasm.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.0.0

flutter:
  uses-material-design: true
